{
  "name": "frontend-inspector",
  "version": "0.1.0",
  "private": true,
  "description": "A Chrome DevTools extension for inspecting frontend components, analyzing renders, debugging runtime issues, and generating structured bug reports.",
  "license": "MIT",
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "typecheck": "tsc -b",
    "typecheck:clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
